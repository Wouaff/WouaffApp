{
  "name": "wouaff",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "start": "npm --prefix server run build:all && node server/dist/index.js",
    "dev": "npm --prefix server run dev",
    "build": "npm --prefix client run build && npm --prefix server run build",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "lint:ci": "biome ci"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12"
  }
}
